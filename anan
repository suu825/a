local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/BlizTBr/scripts/main/Orion%20X')))()

local Window = OrionLib:MakeWindow({
    Name = "Keyステップ", 
    HidePremium = false, 
    SaveConfig = true, 
    ConfigFolder = "ng", 
    IntroEnabled = true, 
    IntroText = "a", 
    IntroIcon = "", 
    Icon = ""
})

local AuraTab = Window:MakeTab({Name = "Keyステップ", Icon = "rbxassetid://117381520745599", PremiumOnly = false})


-- **許可されたKey（パスワード）リスト）**
local ValidKeys = {
    ["iyan.1223"] = true,
}

AuraTab:AddTextbox({
    Name = "キーステップ",
    Default = "",
    TextDisappear = true,
    Callback = function(value)
        -- **入力されたKeyが有効かチェック**
        if ValidKeys[value] then

        else
        end
    end
})

OrionLib:Init()
